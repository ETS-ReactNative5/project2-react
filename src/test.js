let params = {
    params: {}
}

params.params.searchPrompt = "hello";

console.log(params)


if(this.state.currentUserId){
    this.props.species.map(
        eachItem => {
             
        }
    )
}
{this.props.species.map}